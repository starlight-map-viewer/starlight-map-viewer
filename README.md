# Quickstart

1. Run the below to get the virtual env set up.
```
python -m venv .venv
.venv/Scripts/python -m pip install -r requirements.txt
```
2. Run `run.bat`. Set this up with a schedule and it'll do all the work needed.
